# Node modules - exclus (trop gros)
**/node_modules/

# Mais on INCLUT .next pour o2switch (build pré-compilé)
# Ne PAS exclure .next pour permettre le déploiement sur o2switch

# Fichiers locaux
.env.local
*.log
